print("Hello world")

print("An update to initiate the jenkins Poll SCM trigger")

print("Updating, to trigger a change via a pipeline job")

print("A final to trigger a change via a pipeline job")

print("A final to trigger a change via a pipeline job - found a potential issue for the trigger failure")
